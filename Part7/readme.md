# Things to do in class8

# some resources on Fargate
-> create a namespace -> Terraform
-> fargate profile (use that name space ) -> Terraform
-> push a deployment on fargate with Terraform

# some on nodes(managed nodes)

-> deploy the 3-tier app on EKS

-> AWS load balancer controller -> with Terraform
-> backend and frontent with manifest (using kuberctl) -> use argocd later
-> secrets, config from Terraform 
-> ingress Terraform 
-> automate ALB to dns mapping with Terraform
-> automate the ACM ssl cert issue and https ingress




Class 9 Agenda

Terraform stuff
- rds
- config map for rds endpoint 
- secrt for database url

from menifsts